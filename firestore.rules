rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permite lectura y escritura para la app de gestión de restaurante Gastro Smart
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
